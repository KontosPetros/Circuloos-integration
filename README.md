# Circuloos-integration


More information will be uploaded soon



# Issues with behave

The standard output of behave ```behave --or-- behave -f pretty``` does not display the first logging message, so needs to be excecuted as ```behave -f plain```